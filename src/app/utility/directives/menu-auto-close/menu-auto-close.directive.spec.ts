import { MenuAutoCloseDirective } from './menu-auto-close.directive';

describe('MenuAutoCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuAutoCloseDirective();
    expect(directive).toBeTruthy();
  });
});
